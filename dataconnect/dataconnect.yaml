specVersion: "v1beta"
serviceId: "flower-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "flower-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
